import React from 'react';
import {Text, StyleSheet, View} from 'react-native';

const Home = () => {
  return (
    <View>
      <Text>  </Text>
    </View>
  );
};

const styles = StyleSheet.create({
});

export default Home;
